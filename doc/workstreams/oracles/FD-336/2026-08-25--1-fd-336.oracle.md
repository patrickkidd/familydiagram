# FD-336 — oracle (ticket acceptance criteria, ratified by Patrick in chat 2026-08-25)

- C1. Copilot view removed: Pro has no Copilot tab, no CopilotView/CopilotEngine code; the right-drawer chat button opens the embedded Personal tabs instead.
- C2. Discuss/Learn/Plan render in the former Copilot slot of the CaseProperties right drawer behind the same chat icon.
- C2h (human). The embedded Discuss/Learn/Plan beside the canvas look and feel right at desktop size.
- C3. Feature enabled only for server diagrams: local-only .fd → chat unavailable with a reason; owned server diagram → available.
- C4. Personal hamburger/slide-out menu (account drawer, diagram list/create, logout) hidden when embedded in Pro.
- C5. Embedded tabs operate on Pro's already-open Scene/Diagram: one Scene per open case, no diagram load by the Personal pipeline, accepted items appear in the Scene the canvas draws.
- C6. discussion → extract → PDP accept → committed scene data works end-to-end on a Pro server case on the real apps against a sandboxed server; accepted people/events land in the open canvas and persist in the server row.
- C6h (human). Same flow with the real coach/extractor on a real case; survives reopen.
- C7. Exactly one save path: Pro's save and the Personal PDP commit both persist through the same DiagramSaver; concurrent-save interleavings pass with no data loss; Personal's JSON save path is gone.
- C8. Save-before-chat prompt: sending/extracting with unsaved Pro edits prompts to save first; Save completes before the request; Cancel sends nothing; clean stack → no prompt.
- C8h (human). The prompt reads sensibly and is not annoying in normal flow.
- C9. PersonalAppController decomposed into components; when embedded in Pro exactly one Session object exists in-process; no Personal peripherals/prefs until a server case is open.
- C10a. A Person with only name='Connie' renders label 'Connie'.
- C10b. Committing an extracted person yields the Scene's field population (name/lastName); a last-name-only person gets a visible label; rows written with the old key are healed on load.
- C10h (human). Reopen diagram 1924 in the Pro app: every person has a visible label.

Harness oracle (H1–H5, H5h): see `2026-08-26--1-fd-336-harness.oracle.md`.
