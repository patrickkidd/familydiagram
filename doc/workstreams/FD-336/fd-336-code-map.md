I have everything needed; writing the map now.

# FD-336 CODE MAP (master, read-only survey; all paths under `/Users/patrick/theapp/`)

## 1. COPILOT (to be removed)

| Thing | Location |
|---|---|
| `CopilotEngine` (Python) | `familydiagram/pkdiagram/models/copilotengine.py:31-98` — POSTs `/copilot/chat` (l.94-96), signals `requestSent/responseReceived/serverError/serverDown` (l.36-41), `setScene` (l.49). Exported `models/__init__.py:21`. |
| `CopilotView` (Python test helper) | `familydiagram/pkdiagram/views/qml/copilotview.py:10-27`, exported `views/qml/__init__.py:1`. |
| `CopilotView.qml` | `familydiagram/pkdiagram/resources/qml/CopilotView.qml` (264 lines) — `Page`, `Connections { target: copilot }` (l.32-60), `copilot.ask(...)` (l.228). |
| QML context property `copilot` | `familydiagram/pkdiagram/documentview/qmlengine.py:11,42,55,83` (created, registered, `setScene`). |
| Drawer tab registration | `resources/qml/CaseProperties.qml`: root prop `copilotView` l.37; `setCurrentTab` 'copilot'→index 2 l.51-52; `currentTab()` map l.62; title "BT Copilot" l.102; footer `PK.TabButton { text: "Copilot" }` l.130 (4 tabs: Timeline/Settings/Copilot/Triangles); `StackLayout` child `CopilotView { id: copilotView }` l.740-742 (index 2 in stack). Python wrapper `views/caseproperties.py:5`, `QmlDrawer.setCurrentTabIndex` `views/qmldrawer.py:146`. |
| Enum | `documentview/__init__.py:8` `RightDrawerView.Copilot = "copilot"`. |
| Chat icon button | `documentview/toolbars.py:895-899` in `RightToolBar` (class l.863): `PushButton(objectName="copilotButton", pixmap="callout.png", action=self.ui.actionShow_Copilot)`. |
| QAction | `mainwindow/mainwindow_form.py:387-389` (`actionShow_Copilot`, checkable), menu l.496, text/shortcut Ctrl+4 l.782-783. |
| Wiring | `documentview/documentcontroller.py:181` (`toggled→dv.showCopilot`), l.1128-1129 (escape handling); `documentview/documentview.py:450-457` (drawer↔button sync in `setCurrentDrawer`), `showCopilot` l.723-727. |
| Other Copilot refs | `app/session.py:42` `{"attr": "copilot"}` Qt property on Session (unrelated license attr — verify before deleting). `views/qmlbtcopilot.py` is `QmlBTCopilot` license constants — NOT the chat, keep. `mcpbridge/inspector.py` grep hits are `btcopilot` only. |
| Tests | `tests/models/test_copilotengine.py` (whole file), `tests/views/test_copilotview.py` (whole file), `tests/test_documentview.py:1087-1090 test_show_copilot`, `tests/views/test_CaseProperties.py:13` live import + l.281-284 commented fixture, `tests/personal/test_discussview.py:12` commented. |

## 2. PERSONAL CONTROLLER — `familydiagram/pkdiagram/personal/personalappcontroller.py` (2140 lines)

| Responsibility | Lines | Notes |
|---|---|---|
| Imports | 1-58 | `QTextToSpeech`, `QAudioRecorder` (21-22), `Session, Analytics` (50), `Diagram` (52), `Scene/Person/Event/Marriage/Emotion` (53), `SceneModel, PeopleModel` (54), `EventForm` (55), `SARFGraphModel/ShakeDetector/ClusterModel` (56-58). |
| Signals | 64-100 | chat, pdp/diagram/discussion change, extract/rebuild, TTS, transcription. |
| **Session ownership** | 121-124 (`Analytics`, `self.session = Session(self.analytics)`), 399-421 `onSessionChanged` (persists session, starts/stops shake, refreshes diagrams+diagram), 217-235 `init(engine)` (context props + `session.init`), 237-248 `deinit`. | Second Session in-process if instantiated inside Pro. |
| **Own models** | 126-133: `AppConfig(prefsName="personal...")`, `SceneModel`, `PeopleModel`, `SARFGraphModel`, `ClusterModel(session)`. | Pro already has its own SceneModel/PeopleModel in `QmlEngine`. |
| **Scene creation/loading** | `_refreshDiagram` 929-985 (GET `/personal/diagrams/{id}`, base64→`Diagram(**data)`, `Scene()`+`scene.read(pickle.loads(rawData))` 954-966, `setScene`); `loadDiagram` 987-1028 (same, with QMessageBox); `setScene` 360-379 (pushes scene into peopleModel/sceneModel/sarfGraphModel/clusterModel/eventForm, loads clusters from `_diagram.getDiagramData()`). | Personal owns `self.scene` and `self._diagram` (`Diagram` from `server_types`). |
| **Diagram list/create (Personal-only)** | `diagrams` prop 895-897, `_refreshDiagrams` 899-916 (GET `/personal/diagrams`), `diagram` prop 918-922, `_saveLastDiagramId` 924-927, `createDiagram` 1030-1060 (POST `/personal/diagrams/`). | Stays Personal-only per plan. |
| **SAVE path** | `_withSaveGuard` 289-299 (reentrancy queue); `saveDiagram` 301-358: builds its **own `applyChange`** (l.320-347), calls `self._diagram.save(server, applyChange, lambda d: True, useJson=True)` l.349-351 → **JSON/base64 PUT `/personal/diagrams/{id}`**; snapshot baseline `_diagram._lastSavedSnapshot` (l.310-313, 356). Triggered by: `onEventFormSaved` 254, `deleteEvent` 280, `undo` 287, `_onClustersDetected` 397. | Field list quoted in §3. |
| **Discussion controller** | 1062-1198: `discussions/currentDiscussionId/canExtract/canRebuild` props 1062-1092, `createDiscussion/_createDiscussion` 1094-1119 (POST `/personal/discussions/`), `_setCurrentDiscussion` 1121-1127, dirty tracking 1129-1139, `statements` 1147-1152, `sendStatement/_sendStatement` 1154-1198 (POST `/personal/discussions/{id}/statements`, model = `responseModel`). |
| **Extraction / PDP commit** | `_pdpItem` 1200-1207; `acceptPDPItem/rejectPDPItem` 1209-1249 (undo via `HandlePDPItem`, `commands.py:13-37` — undo does `_diagram.setDiagramData(prev)` with NO server save); `_postCommitPdp` 1251-1283 (POST `commit-pdp`, echoes `accepted_through_order`); `_pdpDrained` 1285-1293; **`_doAcceptPDPItem` 1295-1345** (own `applyChange` calling `diagramData.commit_pdp_items([id])`, then `_diagram.save(..., useJson=True)`, then `_addCommittedItemsToScene`); **`_addCommittedItemsToScene` 1347-1413** (the exact PDP→scene function: `Person()/Marriage()/Event()` + `item.read(chunk, byId)` + `scene._dropIrrecoverableEvents` + batch `addItem`); `_doHandleCommittedItem` 1415-1474 (positive-id edits/deletes; `person.setName(...)` 1461); accept/reject committed slots 1476-1498; `_doRejectPDPItem` 1500-1522; `pdp` prop 1524-1540 (adds `committedPeople` from scene); QML label helpers 1543-1681; `dismissEmptyExtraction` 1683-1685; **`acceptAllPDPItems` 1687-1795**; `updatePDPItem` 1797-1834 (setattr on PDP person/event, saves); `clearDiagramData` 1836-1887; journal import 1889-1949 (POST `import-text`, saves PDP via applyChange); **`extractFull` 1951-1994** (POST `extract`, writes PDP locally via `_diagram.setDiagramData` 1972 — no server save; server already persisted it); rebuild 1996-2140 (POST `deep-reextract`, `QTimer.singleShot(1000, _pollRebuild)` 2060/2093, stall watchdog 2104-2116, cancel 2118-2140). |
| **Learn models** | `SARFGraphModel` `personal/sarfgraphmodel.py:14-203` (scene→event/cumulative data), `ClusterModel` `personal/clustermodel.py:30-291` (POST `/personal/diagrams/{id}/clusters` l.244, disk cache 93-135, `clustersDetected`→`saveDiagram`). `pdpChanged→sarfGraphModel.refresh` l.131. |
| **User profile wizard** | 431-566 (`_primaryPerson`, `shouldPromptProfile`, `userProfile`, `saveUserProfile` adds Person/birth Event to scene + `session.updateName`). |
| **Peripherals** | Model select 568-586; **TTS** 154-215 + 588-657 (`QTextToSpeech`, voices, `openSystemVoiceSettings`); **audio/transcription** 659-893 (`QAudioRecorder`, AssemblyAI upload via own `QNetworkAccessManager` l.149, poll `_pollTranscription` 836); **shake** 134-135 (`ShakeDetector`→`undo`; `personal/shakedetector.py:8` optional `QAccelerometer`, cooldown `QTimer` l.25; started/stopped in `onSessionChanged` 402/405). |
| **Hamburger / slide-out** | Not in Python — QML: `resources/qml/Personal/PersonalContainer.qml:115-138` (`objectName:"hamburgerButton"`, `onClicked: drawer.open()`), `Drawer { id: drawer }` l.1252-1298 hosting `Personal.AccountDrawer` (diagram list l.133-226 → `personalApp.loadDiagram/createDiagram` l.1276-1282, settings popups 1283-1294, `session.logout()` 1268). |
| **Timers/threads** | No QThreads. `QTimer.singleShot` polling (rebuild 2060/2093), shake cooldown, `QNetworkAccessManager` async replies. `Session.server().nonBlockingRequest` throughout. |
| EventForm | 257-272 `initEventForm` (from `PersonalContainer.qml:1316 Component.onCompleted: personalApp.initEventForm(eventForm)`), `editEvent` 267. Pro has its own `EventFormDrawer` (`documentview.py:121`). |

Personal entry point: `familydiagram/pkdiagram/main.py:157-207` (`PersonalAppController()` l.165, `app.personalController` l.166, `QQmlApplicationEngine` + `controller.init(engine)` l.171-173, loads `resources:qml/PersonalApplication.qml` l.175, `extensions.setActiveSession` l.179, `PersonalDevMenu` l.184-187).

## 3. SAVE PATHS (two client writers + one server-side writer on one `diagrams.data` row)

### Pro — `familydiagram/pkdiagram/models/serverfilemanagermodel.py` (621 lines)
- Trigger: `mainwindow/mainwindow.py:660-694 MainWindow.save()` — writes local doc l.667-669 (`document.updateDiagramData`), then if `scene.serverDiagram()` → `serverFileModel.setData(index, bdata, DiagramDataRole)` l.671-683; local `.fd` falls to `document.save` l.691.
- `setData` l.480-591; DiagramDataRole branch l.495-586: snapshot baseline `diagram._lastSavedSnapshot or diagram.data` l.508-509; **`applyChange` l.511-561**; `diagram.save(..., useJson=False)` l.565-567 → **pickle PUT `/v1/diagrams/{id}`**; on success stores `_lastSavedSnapshot = dataToSave` l.574; failure → `QMessageBox` l.580-584.
- `reload` l.130-132 (clear cache + `update`); `update` l.189-273 (GET `/diagrams` index, per-diagram GET when `saved_at` newer); `syncDiagramFromServer` l.278-299 (blocking GET, `_addOrUpdateDiagram`); `_addOrUpdateDiagram` l.334-397 writes hashed `diagram.pickle` to disk l.369-374 and **replaces the cached `Diagram` object when server is newer l.389-390** — 30-min poll `mainwindow.py:86-88,348-350,1270-1274`. Inference: a replaced cache object drops `_lastSavedSnapshot` (set on the old object at l.574), so the next Pro save's baseline becomes server-canonical data; per `apply_local_changes` semantics items in that baseline but absent from Pro's Scene read as DELETED. Not verified by test; worth a concurrent-save case.
- Pro `applyChange` field list (l.519-560): `SCENE_COLLECTION_FIELDS` via `apply_local_changes` (people, events, pair_bonds, emotions, multipleBirths, layers, layerItems, items, pruned) + `uuid, name, tags, loggedDateTime, masterKey, alias, version, versionCompat, lastItemId(max), readOnly, contributeToResearch, useRealNames, password, requirePasswordForRealNames, showAliases, hideNames, hideToolBars, hideEmotionalProcess, hideEmotionColors, hideDateSlider, hideVariablesOnDiagram, hideVariableSteadyStates, hideSARFGraphics, exclusiveLayerSelection, storePositionsInLayers, currentDateTime, scaleFactor, pencilColor, eventProperties, legendData`. **Excluded (pass through from server): `pdp`, `clusters`, `clusterCacheKey`, `id`.** Comment l.512-513 cites FR-2 `doc/specs/DATA_SYNC_FLOW.md:30`. (`DATA_SYNC_FLOW.md:424-430,457-462` describes a `metadata={"personal":True}`/`personalFields()` mechanism that no longer exists in code — stale doc.)

### Personal — `personalappcontroller.py:301-358` `applyChange` (l.320-347)
Writes: `SCENE_COLLECTION_FIELDS` via `apply_local_changes` + `version, versionCompat, name, lastItemId(max), clusters, clusterCacheKey`. **Everything else (uuid, tags, all UI flags, eventProperties, legendData, pdp) passes through from server.** PDP-commit `applyChange`s (1300-1330, 1730-1770, 1420-1445, 1870-1878) mutate `pdp/people/events/pair_bonds/lastItemId` only.
Also client-local partial writer: `server_types.py:319-330 Diagram.setDiagramData` writes only `pdp, lastItemId, people, events, pair_bonds, clusters, clusterCacheKey` into the pickled dict (used by `extractFull` 1972 and `HandlePDPItem.undo`).

### Shared client `Diagram.save` — `familydiagram/pkdiagram/server_types.py:205-309`
Retry loop 3× l.215; `getDiagramData()`→`applyChange`→`pickle.dumps(asdict(...))` l.216-220; `useJson=True` → JSON `{data: b64, expected_version}` to `/personal/diagrams/{id}` l.222-229; else pickle `{data, updated_at, expected_version}` to `/v1/diagrams/{id}` l.232-240; PUT with `statuses=[200,409]` l.243-251; on 200 sets `version` and canonical `data` (falls back to `newData` when server omits it) l.268-288; on 409 refreshes `version/data`, `stillValidAfterRefresh` (every caller passes `lambda d: True`) l.290-306. `getDiagramData` l.311-317 loads all known fields.

### Server — `btcopilot/btcopilot/`
- Pro pickle endpoint `pro/routes.py:152-280` `PUT|PATCH /v1/diagrams/<id>` l.234-273: `pickle.loads(request.data)`, sets `updated_at` from client l.242, `update_with_version_check(expected_version, new_data=data["data"])` l.244-246, 409 returns `{version, data}` pickled l.248-255, 200 returns canonical `{version, data}` l.270-273. Whole-blob replace, no server-side field filtering.
- Personal JSON endpoint `personal/routes/diagrams.py:96-137` `PUT /personal/diagrams/<id>`: base64 body l.114-120, same `update_with_version_check` l.122-124, 409 JSON `{version, data(b64)}` l.126-133, **200 returns `{success, version}` only (no canonical data)** l.137.
- `pro/models/diagram.py:215-237 update_with_version_check` — `new_data` whole blob OR `diagram_data` → merges only `pdp, lastItemId, people, events, pair_bonds` into existing pickled dict l.224-230; SQL `version = version + 1` guarded by expected version l.234-237. `set_diagram_data` l.93-108 (same 5 keys).
- **Server-side writers to the same row** (third writer class): `personal/routes/discussions.py:116-119` (extract stores PDP), `:285-366 commit-pdp` (32× retry `update_with_version_check(diagram_data=...)` l.327-366, `apply_parent_edits` l.352), `personal/routes/diagrams.py:189` (import-text), `:38` (create).

## 4. SCHEMA — `btcopilot/btcopilot/schema.py`
- `Person` l.295-301: `id, name, last_name, gender: PersonKind, parents, confidence`. No `first_name`. `PersonKind` l.174-179 (`male/female/abortion/miscarriage/unknown`).
- `DiagramData` l.425-471 (fields quoted in §3 + `id`, `clusters`, `clusterCacheKey`, `pdp: PDP`, `lastItemId`); `SCENE_COLLECTION_FIELDS` l.473-483; `apply_local_changes` l.485-550 (id-keyed snapshot-diff, item-level LWW).
- **Where a committed person chunk gets its flat `name`**: `commit_pdp_items` l.614-749 → `self.people.append(asdict(new_person))` **l.711** — chunk keys are exactly the `Person` dataclass fields (`id,name,last_name,gender,parents,confidence`). Extraction output is parsed as `from_dict(Person, ...)` (`pdp.py:997,1316`); prompts live in fdserver (not in this tree: `fdserver/btcopilot/` is empty here).
- familydiagram-side PDP→scene mapping: `personalappcontroller.py:1347-1413 _addCommittedItemsToScene` (`Person.read(chunk)` → `scene/person.py:385-433`, generic `scene/item.py:137-145 Item.read` sets each registered prop from `chunk.get(attr)`; unknown keys are retained in `_readChunk` and re-written on `Item.write` l.128-134).
- Field-sync rule anchors: `familydiagram/pkdiagram/scene/scene.py:1484-1527 Scene.diagramData()` (constructs every non-personal field; omits `pdp/clusters/clusterCacheKey/id`), `serverfilemanagermodel.py:511-561`.

## 5. LABEL — `familydiagram/pkdiagram/scene/person.py`
- Props: `name` (= first name; `PersonProperties.qml:172 firstNameEdit` binds it), `middleName`, `lastName`, `nickName`, `birthName`, `alias` l.101-106; `showLastName/showMiddleName` default True l.119-130; `hideDetails` default False l.142. There is **no `firstName` attribute anywhere** (`grep firstName` → only QML ids and PAC profile dict 487-524).
- Label: `fullNameOrAlias` l.463-480 (hideNames→""; showAliases→"[alias]"; else `name` + middle + last + nick); `updateDetails` l.1328-1345 appends `fullNameOrAlias()` unless `hideDetails`; `firstNameOrAlias` l.451-461; `itemName` l.498.
- Snake→camel only on load: `models/compat.py:781-783` renames `last_name`→`lastName` inside `Scene.read` (`scene.py:1158 compat.update_data`). `_addCommittedItemsToScene` does **not** run compat, so `last_name` is dropped until the next reload.
- **Empirical check (temp pytest, deleted after run):** chunk `{"id":5,"name":"Connie","last_name":None,"gender":"female","parents":None,"confidence":0.9}` → both direct `Person.read`+`addItem` and `Scene.read` yield `name='Connie'`, `fullNameOrAlias()='Connie'`, `detailsText.text()='Connie'`, visible=True. **The ticket's stated mechanism ("scene derives label from firstName/lastName so 'Connie' gets no label") does not reproduce on master.** Diagram 1924 snapshot referenced in `doc/plans/2026-05-02--auto-arrange-layout.md:123` (`/tmp/diagram-1924-snapshot-...pkl`) no longer exists; builder must pull 1924's people chunks (name/lastName/alias/hideDetails + scene `hideNames/showAliases/useRealNames`) before touching the writer. Note `mainwindow.py:1195-1200` forces `showAliases` when `useRealNames && requirePasswordForRealNames`, and `hideNames` blanks every label — both are scene flags, not per-person.

## 6. QML
- Personal tabs: `resources/qml/Personal/DiscussView.qml` (782 l., `Page`, ~28 `personalApp.*` calls l.74-781, plus `pdpSheet`), `LearnView.qml` (2573 l., uses `clusterModel`/`sarfGraphModel` only l.21-338), `PlanView.qml` (25 l., placeholder). Host: `Personal/PersonalContainer.qml` (1517 l.): header l.97-385 (hamburger 115-138, discussion/story/import dropdowns, extract/rebuild buttons 247-330), `StackLayout { id: stack }` l.387-415 with the three views, custom `tabBar` l.418-495 (`Repeater model: ["Discuss","Learn","Plan"]`, objectNames `discussTab/learnTab/planTab`), overlays/drawers 497-1517 (`pasteTextDrawer` 869, account `drawer` 1252, `eventFormDrawer` 1300-1318, profile/model/voice popups). Root window: `resources/qml/PersonalApplication.qml:10-61` (`ApplicationWindow` → `Personal.PersonalContainer` l.54-56). Imports used by Personal QML: `"../PK"`, `"." as Personal`, `".." as Root` (DiscussView l.9-11) — resolvable from `resources/qml/`.
- Pro drawer: `CaseProperties.qml` (§1) — `PK.Drawer` with `header: PK.ToolBar` l.85-123, `footer: PK.TabBar` l.125-134, `StackLayout { id: stack }` l.144+; page index ↔ `setCurrentTab/currentTab` l.46-64; Python side `documentview.py:81-88` constructs `CaseProperties(self._qmlEngine, "qml/CaseProperties.qml")`, `onCasePropsInit` l.249-258 hooks `stack.currentIndexChanged`.
- Context properties — Personal (`personalappcontroller.py:218-226`): `CUtil, util, session, personalApp, sceneModel, peopleModel, sarfGraphModel, clusterModel`. Pro (`documentview/qmlengine.py:53-67`): `engine, util, copilot, btcopilot, session, sceneModel, searchModel, timelineModel, eventSelectionModel, peopleModel, accessRightsModel`. Missing in Pro for the embedded views: `personalApp, sarfGraphModel, clusterModel, CUtil`. `tests/personal/test_discussview.py:36-53` already shows DiscussView running on Pro's `qmlEngine` fixture by injecting those four.

## 7. APP WIRING
- Pro: `main.py:213-219` `Application(Type.Pro)` → `AppController(app)` (`app/appcontroller.py:56 self.session = Session(self._analytics)`) → `MainWindow(appConfig, session)` (`mainwindow.py:93,103`). `MainWindow.documentView = DocumentView(self, self.session)` l.180; `DocumentView.__init__` `documentview.py:45-53` creates `QmlEngine(session)` (`qmlengine.py:25-67`). Scene ownership: `MainWindow.scene` — built in `setDocument` `mainwindow.py:1026 Scene(document=document)` + `newScene.read(data)` l.1041-1048, `setScene` l.1130-1138 → `documentView.setScene(scene)` l.1206 → `QmlEngine.setScene` (`documentview.py:291`) fans out to models. Server open: `onServerFileClicked` l.830-849 sets `_isOpeningServerDiagram`, `open(fpath)`, `qmlEngine().setServerDiagram(diagram)` l.840 (→ `scenemodel.py:59-62` → `scene.setServerDiagram`), `ServerBlockAllocator` bound l.1222-1236, poll timer l.1220.
- Personal: `main.py:157-207` (§2).

## 8. SERVER-VS-LOCAL
`familydiagram/pkdiagram/scene/scene.py:334-338` `Scene.setServerDiagram/serverDiagram()` (`_serverDiagram` init l.234; None for local `.fd`). Consumers: `mainwindow.py:511-513,672,913,1272`; QML-facing `SceneModel.isOnServer` (`models/scenemodel.py:76-80`) and `isMyDiagram` l.81-86; `mcpbridge/inspector.py:2101`. Set only via `mainwindow.py:840`. Transient flag `MainWindow._isOpeningServerDiagram` l.838-848,994-1006.

## 9. TESTS
- Pro save/merge: `tests/models/test_serverfilemanagermodel.py` — real-model saves `test_save_free_diagram_persists` 247, `test_save_preserves_server_pdp/clusters` 326/349, `test_save_merges_correctly_on_409_retry` 366, `test_save_scene_roundtrip_fidelity` 403, `test_save_empty_pdp_stays_empty` 425; concurrent tests 553-732 run against **test-local copies** `_pro_apply_change` 459 / `_personal_apply_change` 520 (comment l.443-448), not the production `applyChange`s — a DiagramSaver must be tested directly or these stay decoupled.
- Personal: `tests/personal/conftest.py:9-40` (`personalApp` fixture loads `PersonalApplication.qml`), `test_personalappcontroller.py` (save error 266, commit-pdp 422/453, committed edits 1288-1489), `test_pdp_2_scene.py` 30-494 (accept→scene), `test_pdp_commit.py` 13-224 (schema commit), `test_discussview.py`, `test_personalview.py`, `test_clustermodel.py`, `test_sarfgraphmodel.py`, `test_tts.py`, `test_userprofile.py`.
- Copilot: §1. Person label: `tests/scene/test_person.py:105-185` (`detailsText` fixture/asserts on `person.name()`); no single-token/name-only test.
- btcopilot: `tests/pro/test_mvp_merge_integration.py:55-225` (stale save, delete survival, block allocation, canonical blob), `tests/pro/test_diagram_pickle_format.py`, `tests/personal/test_diagrams.py:91-194` (JSON PUT + optimistic locking), `test_reextract_commit.py`, `test_reextract_concurrency.py`, `test_reextract_cursor.py`.
- MCP bridge: `pkdiagram/mcpbridge/server.py:112,272-275,577-579` (`save_diagram` → `inspector.py:2058-2105 saveDiagram`: Personal→`controller.saveDiagram()`, Pro→`mainWindow.save()`, counts 409 log lines), `open_server_diagram` `server.py:543-548` → `inspector.py:1389-1466` (Pro: `syncDiagramFromServer`+`onServerFileClicked`; Personal: `_refreshDiagram` via `lastDiagramId`). MCP tools `familydiagram/mcpserver/mcp_server.py:993 launch_app, 1206 open_server_diagram, 1221 save_diagram, 1236 get_status, 1249 wait_until_idle, 1788 seed_server_data, 1826 personal_state, 1840 inject_pdp_data, 1859 open_pdp_sheet`; ephemeral server `mcpserver/ephemeral_server.py:70 /test/seed, 165 /test/diagrams/seed_pickle`. Docs `familydiagram/CLAUDE.md:132-188`. **No existing concurrent-save journey test exists in `pkdiagram/tests/mcpbridge/`** (only `inspector.py`, `server.py`); the concurrent cases live in pytest above.

## Builder-relevant discrepancies found
1. Label bug mechanism in ticket not reproducible on master (§5) — needs data from diagram 1924 before a writer change.
2. Schema/scene key mismatch that IS real: `last_name`→`lastName` only via load-time compat (`compat.py:782`), not on live commit (`_addCommittedItemsToScene`).
3. Three writer classes on the row (Pro pickle, Personal JSON, server-side routes using `update_with_version_check(diagram_data=)` 5-key merge) — "single save owner" on the client still leaves server-side commit-pdp/extract/import as concurrent writers.
4. Personal PUT returns no canonical blob (`diagrams.py:137`) → client keeps its own post-merge bytes as `diagram.data` (`server_types.py:283-285`); Pro PUT does return it.
5. Pro's cached `Diagram` object is replaced by the 30-min poll (`_addOrUpdateDiagram:389-390`), losing `_lastSavedSnapshot` (inference: baseline regresses to server-canonical → possible spurious deletes on next merge).
6. `DATA_SYNC_FLOW.md:424-462` describes a `personalFields()` metadata mechanism that no longer exists.